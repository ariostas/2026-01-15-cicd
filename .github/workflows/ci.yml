name: My CI

on:
  push:
    branches: [main]

jobs:
  hello:
    name: Hello job
    runs-on: ubuntu-latest
    steps:
      - name: My first step
        run: echo "Hello World"

  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - name: Install Python package
        run: pip install .[test]
      - name: Run tests
        run: pytest -vv
